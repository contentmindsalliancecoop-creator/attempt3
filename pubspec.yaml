name: attempt3
description: Attempt 3 app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

# --- SECCIÓN DE DEPENDENCIAS ---
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  audioplayers: ^6.0.0
  shared_preferences: ^2.2.3
  http: ^1.2.2
  package_info_plus: ^8.0.0
  path_provider: ^2.1.4
  dio: ^5.4.3+1
  open_file_plus: ^3.4.1 # <-- Este es el correcto que estamos usando

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# --- SECCIÓN DE FLUTTER ---
flutter:
  uses-material-design: true

  assets:
    - assets/audio/